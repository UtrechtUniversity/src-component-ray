---
- name: Prepare
  hosts: all
  gather_facts: false
  tasks:
    # You can add tasks here that will be run in molecule's `prepare` phase
    - name: Install required pkgs
      ansible.builtin.package:
        name:
          - iproute2
        state: present
